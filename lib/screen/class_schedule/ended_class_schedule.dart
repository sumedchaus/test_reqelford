import 'package:flutter/material.dart';

class EndedClassScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("EndedClass"),
    );
  }
}
